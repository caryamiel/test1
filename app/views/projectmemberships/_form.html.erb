<%= form_for(@projectmembership) do |f| %>
  <% if @projectmembership.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@projectmembership.errors.count, "error") %> prohibited this projectmembership from being saved:</h2>

      <ul>
      <% @projectmembership.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :projectmembership_id %><br>
    <%= f.number_field :projectmembership_id %>
  </div>
  <div class="field">
    <%= f.label :project_id %><br>
    <%= f.number_field :project_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
